# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'dartsNoTabi' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for dartsNoTabi
  pod 'PromiseKit/CorePromise'
  pod 'Kingfisher'
  pod 'CryptoSwift'
  pod 'Sourcery'

  target 'dartsNoTabiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'dartsNoTabiUITests' do
    # Pods for testing
  end

end
